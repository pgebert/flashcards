package flashcards

/**
 * User commands
 *
 * @constructor Create new Command
 */
enum class Command {
    ADD,
    REMOVE,
    IMPORT,
    EXPORT,
    ASK,
    EXIT,
    LOG,
    HARDEST_CARD,
    RESET_STATS
}